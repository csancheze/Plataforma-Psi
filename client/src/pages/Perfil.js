import React from "react";
import { Link } from "react-router-dom";

const Perfil = () => {

    return(
        <main className="container">
            <p>Esta es la pagina para editar el perfil</p>
        </main>
    )
};

export default Perfil